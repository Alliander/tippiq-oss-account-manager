/**
 * HoodUserServicePreferenceModel.
 * @module users/hood-user-service-preference-model
 */


import { HoodBaseModel } from '../base-model';

const instanceProps = {
  tableName: 'user_service_preference',
};

const classProps = {};

export default HoodBaseModel.extend(instanceProps, classProps);
