/**
 * HoodUserPlaceModel.
 * @module users/hood-user-place-model
 */


import { HoodBaseModel } from '../base-model';

const instanceProps = {
  tableName: 'user_place',
};

const classProps = {};

export default HoodBaseModel.extend(instanceProps, classProps);
